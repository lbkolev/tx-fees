name: CI
on:
  push:
    branches: [master]
  pull_request:

jobs:
  ci:
    name: verify fmt/lints/build/tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - run: cargo build --release
      - run: cargo fmt -- --check
      - run: cargo clippy -- -D warnings
      - run: cargo test --all-features
